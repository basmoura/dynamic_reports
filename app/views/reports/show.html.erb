<p id="notice"><%= notice %></p>

<p>
  <strong>Takeaway points:</strong>
  <div class="summernote">
    <ul>
      <% @report.takeaway_points.reject(&:empty?).each do |r| %>
        <li><%= t r %></li>
      <% end %>
    </ul>
  </div>
</p>

<%= link_to 'Edit', edit_report_path(@report) %> |
<%= link_to 'Back', reports_path %>
